const colors = {}

const common = {
  flexCenter: `
    display: flex;
    align-items: center;
    justify-content: center;
  `,
  ErrorMessage: `
    color: tomato;
    font-size: 0.8rem;
  `,
}

const theme = {
  colors,
  common,
}

export default theme
