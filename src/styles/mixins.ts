import { css } from 'styled-components';

export const inputInPage = css`
  ${({ theme }) => css`
    height: 4.4rem;
    padding: 1rem 1.2rem;
  `}
`;

export const cardSize = css`
  ${({ theme }) => css`
    width: 100%;
    height: 18rem;
  `}
`;
